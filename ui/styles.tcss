StatusBar {
    dock: top;
    height: 1;
    background: $surface;
    padding: 0 1;
}

#transcript {
    height: 1fr;
    padding: 0 1;
}

AgentPanel {
    height: auto;
    max-height: 8;
    background: $surface;
    padding: 0 1;
    border-top: solid $accent;
}

Input {
    height: 3;
}
